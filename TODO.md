# TODO

## jq.test 移植状況

- 総テスト: 532 (514 + 18 %%FAIL)
- 移植済み: 334
- 除外: 129
- jq 1.8.1 互換率: 178/188 = 94.7%

## 未実装機能 (除外テスト 129件)

### 代入・パス操作 (59件)

path expression トラッキングが必要。jq の中で最も複雑な機能。

- `=` 代入 (26件): `.foo = 1`, `.[0] = "x"`
- `|=` 更新代入 (14件): `.foo |= . + 1`, `.[] |= select(. > 2)`
- `setpath` (6件): `setpath(["a","b"]; 1)`
- `del` (5件): `del(.foo)`, `del(.[2:4])`
- `pick` (4件): `pick(.a.b.c)`
- `delpaths` (3件): `delpaths([["a"]])`
- `//=` 代替代入 (1件): `.[] //= .[0]`

### 分割束縛・代替分割 (30件)

- `?//` 代替分割 (17件): `. as {$a} ?// [$a] ?// $a | ...`
- `as [$a, $b]` 配列分割 (9件): `.[] as [$i, $j] | ...`
- `as {$a}` オブジェクト分割 (4件): `. as {a: $a} | ...`

### 文字列補間 (10件)

- `\(expr)` 構文: `"Hello \(.name)"`
- レキサーの文字列パース内で式を再帰パースする必要あり

### モジュールシステム (9件)

- `import/include` (9件): jq のモジュール読み込み機能

### path 式 (8件)

- `path(expr)`: フィルタが辿るパスを返す
- `paths(f)` の高度な使い方

### 制御フロー (6件)

- `label $out | ... | break $out`: ラベル付きブレイク
- 非ローカル脱出の仕組みが必要

### その他 (7件)

- `@csv`, `@tsv`, `@sh`, `@urid` フォーマット (2件)
- `utf8bytelength` (2件)
- `getpath` の高度な使い方 (1件)
- `$__loc__` (1件)
- `input` (1件)

## 既知の互換性差異 (修正困難)

- 巨大整数 (>2^53) が Infinity になる (Double 精度限界)
- `-0` が `0` として出力される
- NUL 文字 (`\u0000`) を含む文字列の `contains` が不正確
- `fromjson` のエラーメッセージが jq と異なる
- エラーメッセージのフォーマットが jq と微妙に異なる (`Cannot index` vs `cannot index`)
